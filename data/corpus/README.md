# HALT-RAG Final Corpus

Place final corpus files here as `.json`, `.jsonl`, `.txt`, or `.md`.

If this directory has no source documents, the backend automatically falls
back to `data/sample_corpus.json`. Runtime files generated here include:

- `halt_rag.faiss`
- `embeddings.npy`
- `metadata.jsonl`
- `dynamic_knowledge.jsonl`

